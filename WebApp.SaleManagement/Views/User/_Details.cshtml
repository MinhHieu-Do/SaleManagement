@model WebApp.SaleManagement.ViewModels.UserViewModel
<div class="modal-header">
<h3 class="modal-title">User Role</h3>
<button type="button" class="close" data-dismiss="modal" aria-label="Close">
    <span aria-hidden="true">&times;</span>
</button>
   </div>
<div>
    
    <br />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Id)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Id)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Email)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Email)
        </dd>
    </dl>

</div>

<form method="post" asp-action="Details">

    <input type="hidden" asp-for="@Model.Id" />

    @*@foreach (var role in Model.UserInRoles)
        {
            <input type="hidden" asp-for="@role.Id" />
            <input type="checkbox" asp-for="@role.Select" />
            <label>@role.Name</label><br />
        }*@
    @for (int i = 0; i < Model.UserInRoles.Count; i++)
    {
        @Html.HiddenFor(m => Model.UserInRoles[i].Id)
        @Html.CheckBoxFor(x => Model.UserInRoles[i].Select)
        <label>@Model.UserInRoles[i].Name</label><br />
    }

    <button type="submit" value="Update" class="btn btn-sm btn-outline-primary" ><i class="fe fe-activity"> Update</i></button>
</form>
<div>
   
    <a class="text-info" asp-action="Index">Back to List</a>
</div>
